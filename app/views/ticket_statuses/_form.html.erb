<%= form_with(model: ticket_status, class: "contents") do |form| %>
  <% if ticket_status.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(ticket_status.errors.count, "error") %> prohibited this ticket_status from being saved:</h2>

      <ul class="list-disc ml-6">
        <% ticket_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :name %>
    <%= form.text_field :name, class: ["block shadow-sm rounded-md border px-3 py-2 mt-2 w-full", {"border-gray-400 focus:outline-blue-600": ticket_status.errors[:name].none?, "border-red-400 focus:outline-red-600": ticket_status.errors[:name].any?}] %>
  </div>

  <div class="my-5 flex items-center gap-2">
    <%= form.label :is_default %>
    <%= form.check_box :is_default, class: ["block shadow-sm rounded-md border order-first h-5 w-5", {"border-gray-400 focus:outline-blue-600": ticket_status.errors[:is_default].none?, "border-red-400 focus:outline-red-600": ticket_status.errors[:is_default].any?}] %>
  </div>

  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
